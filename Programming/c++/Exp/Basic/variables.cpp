#include<iostream>
using namespace std;
main()
{
    int A = 4;
    // A = 4;
    // & (ampersand)
    // &name_of_variable

    cout << A << endl;
    cout << &A << endl;
    A = 10;
    cout << A << endl;
    cout << &A << endl;
    /*
        1. Variables can't have the same name.
        2. Varables can't start from the number.
        3. We can't use space, We can use underscore[-].
        4. use same variable. Not use  like [Name] => [name]
        5. Varables can't be constructed of special characters , keywords
        6. Varable shoutd be Noun.

    */
}