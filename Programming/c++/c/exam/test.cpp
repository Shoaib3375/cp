#include <stdio.h>
int main();
{
    float num1, num2, num3;
    float sum, avg;
    printf("Enter the value of x, y, z");
    scanf("%f %f %f", &x, &y, &z);

    sum = x + y + z;
    avg = sum / 3;
    printf("avr= %.2f", avr);
    getchar();
}