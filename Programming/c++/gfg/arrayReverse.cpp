#include <bits/stdc++.h>
using namespace std;
int arr[5];

int main()
{

    // int sz = sizeof(arr) / sizeof(arr[0]);
    // cout << sz;
    for (int i = 0; i < 5; i++)
    {
        cin>> arr[i];
        
    }
    for (int i = 4; i>=0; i--)
    {
        cout << arr[i];
    }
    


}