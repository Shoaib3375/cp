#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define do double
main(){
    do x,p,op;
    cin >> x>>p;
    op = 100 - x;
    do price = 1.0 *(100 * p)/op;
    cout << fixed<<setprecision(2)<<price; 
}